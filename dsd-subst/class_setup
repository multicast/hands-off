#
d-i preseed/include_command     string ( set -x ; IFS=';'; for cls in $(debconf-get local/classes); do [ "$cls" ] && preseed_fetch "$(dirname $last_location)/${cls}/subclasses" /tmp/cls-$cls; done ; sleep 10000 || true ) > /var/log/class_setup.debug 2>&1
